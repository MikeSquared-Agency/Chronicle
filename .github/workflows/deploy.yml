name: Deploy

on:
  workflow_dispatch:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: [self-hosted, openclaw]
    steps:
      - run: cd ~/chronicle && git pull origin main
      - run: cd ~/chronicle && docker build -t chronicle:latest .
      - run: docker service update --force warren_chronicle
